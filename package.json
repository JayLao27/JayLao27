{
  "name": "readme-animations",
  "version": "1.0.0",
  "description": "Fade-in animations for README",
  "scripts": {
    "build": "tsc src/fade-in.ts --target ES2020 --module ES2020 --outDir src --lib ES2020,DOM"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}

