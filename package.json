{
  "name": "animated-contributions",
  "version": "1.0.0",
  "description": "Animated contributions timeline visualization",
  "type": "commonjs",
  "scripts": {
    "build": "tsc contributions.ts --target ES2020 --module ES2020 --outDir .",
    "watch": "tsc contributions.ts --target ES2020 --module ES2020 --outDir . --watch",
    "generate-svg": "node generate-svg.js"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "ts-node": "^10.9.2",
    "@types/node": "^20.0.0"
  }
}
